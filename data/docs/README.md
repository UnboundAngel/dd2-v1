# dd2/data/

This folder stores structured DD2 data for tools and UIs.

General rules:
- Prefer a single canonical file per logical domain (e.g., items, heroes, builds).
- Use clear, explicit field names.
- Do not silently change field meanings; update docs when necessary.
